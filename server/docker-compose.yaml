services:
  mongodb:
    image: mongo:7.0
    container_name: studysprint-mongodb
    restart: unless-stopped
    environment:
      MONGO_INITDB_ROOT_USERNAME: ${MONGODB_USER:-studysprint}
      MONGO_INITDB_ROOT_PASSWORD: ${MONGODB_PASSWORD:-studysprint123}
      MONGO_INITDB_DATABASE: ${MONGODB_DATABASE:-studysprint}
    ports:
      - '27017:27017'
    volumes:
      - mongodb_data:/data/db
      - mongodb_config:/data/configdb
      - ./init-scripts:/docker-entrypoint-initdb.d
    networks:
      - studysprint-network
    healthcheck:
      test: echo 'db.runCommand("ping").ok' | mongosh localhost:27017/studysprint --quiet
      interval: 10s
      timeout: 5s
      retries: 5

  mongo-express:
    image: mongo-express:latest
    container_name: studysprint-mongo-express
    restart: unless-stopped
    ports:
      - '8081:8081'
    environment:
      ME_CONFIG_MONGODB_ADMINUSERNAME: ${MONGODB_USER:-studysprint}
      ME_CONFIG_MONGODB_ADMINPASSWORD: ${MONGODB_PASSWORD:-studysprint123}
      ME_CONFIG_MONGODB_URL: mongodb://${MONGODB_USER:-studysprint}:${MONGODB_PASSWORD:-studysprint123}@mongodb:27017/
      ME_CONFIG_BASICAUTH_USERNAME: ${MONGODB_USER:-studysprint}
      ME_CONFIG_BASICAUTH_PASSWORD: ${MONGODB_PASSWORD:-studysprint123}
    networks:
      - studysprint-network
    depends_on:
      mongodb:
        condition: service_healthy

volumes:
  mongodb_data:
    driver: local
  mongodb_config:
    driver: local

networks:
  studysprint-network:
    driver: bridge
